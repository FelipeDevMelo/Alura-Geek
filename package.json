{
    "name": "my-project",
    "version": "1.0.0",
    "scripts": {
      "start": "vercel dev",
      "build": "echo 'Skipping build step as this is a static site'",
      "vercel-build": "echo 'Skipping build step as this is a static site'"
    },
    "devDependencies": {
      "json-server": "^0.16.3"
    },
    "dependencies": {
      "@vercel/node": "^1.12.1"
    }
  }
  